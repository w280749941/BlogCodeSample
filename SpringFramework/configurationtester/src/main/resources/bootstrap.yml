spring:
  application:
    name: tester
  cloud:
    config:
      discovery:
        service-id: CONFIG
        enabled: true
      profile: dev
  rabbitmq:
    host: 192.168.1.233
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8081
